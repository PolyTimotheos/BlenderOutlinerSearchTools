Adds a select button next to the search bar and a quick filter drop down to filter what types of objects to select.

Select Button = select all objects that match the search and filter criteria
Select Button + Ctrl = select only visible
Select Button + Shift = add to current selection
Select Button + Alt = remove from current selection

<img width="648" height="675" alt="image" src="https://github.com/user-attachments/assets/529b0c7d-1d15-405a-8c79-a1796576f028" />
